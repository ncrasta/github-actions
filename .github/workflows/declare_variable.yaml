name: Variables
on:
  push:

jobs:
  run-github-actions:
    name: Run Github Actions
    runs-on: ubuntu-20.04
    steps:
      - name: Step 1
        id: step1
        run: echo "Step 1"

      - name: Store result
        run: |
          out=$( steps.step1.status == "success" )
          echo "OUT=$out" >> $GITHUB_ENV

      - name: Print
        run: echo $OUT

